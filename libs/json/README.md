# JSON Library

This library will help you parse JSON files.

In the current state, it's really simple and does the very basic parsing task. It uses the eval primitive to create a useable data structure for Soluna.

## Functions

- `(json-parse-file String)`

## Other functions

They are mainly there to be used inside `json-parse-file`. You might want to use them to create your own parser.
